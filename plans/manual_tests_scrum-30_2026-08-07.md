# Manual Test Cases — SCRUM-30
**Ticket**: SCRUM-30 — [FE] Add "Bulk Edit Mode" to Bulk Update (Facility + Payer / Resident + Payer Category)
**Date**: 2026-08-07
**Module**: bulk_update

| # | AC | Type | Priority | Scenario | Pre-conditions | Test Data | Steps | Expected Result |
|---|----|------|----------|----------|----------------|-----------|-------|-----------------|
| 1 | AC1 | Happy Path | High | Bulk Mode toggle displays both options | User logged in as Admin; Navigate to https://revflow-dev.axgsolutions.com/tasks/bulk-edit | N/A | 1. Navigate to the Bulk Edit Tasks screen.<br>2. Verify the filter bar is visible.<br>3. Verify a "Bulk Mode" toggle control is present in the filter bar.<br>4. Verify the toggle shows exactly two option labels: "Facility + Payer" and "Resident + Payer Category". | 1. Bulk Edit Tasks screen loads successfully.<br>2. Filter bar is visible.<br>3. "Bulk Mode" toggle control is visible in the filter bar.<br>4. Both "Facility + Payer" and "Resident + Payer Category" labels are visible on the toggle. |
| 2 | AC2 | Happy Path | High | Facility + Payer is the default mode on page load | User logged in as Admin; fresh page load, no prior filter state | N/A | 1. Navigate to the Bulk Edit Tasks screen for the first time in the session.<br>2. Verify which Bulk Mode option is visually active on the toggle.<br>3. Verify the two filter dropdowns displayed. | 1. Screen loads with filter bar visible.<br>2. "Facility + Payer" option is shown as the active/selected mode.<br>3. Facility dropdown and Payer dropdown are displayed. |
| 3 | AC3 | Happy Path | High | Facility + Payer mode shows Facility and Payer dropdowns | Bulk Mode is set to "Facility + Payer" | N/A | 1. Verify Bulk Mode toggle shows "Facility + Payer" as active.<br>2. Verify the label of the first filter dropdown.<br>3. Verify the label of the second filter dropdown. | 1. "Facility + Payer" is the active mode.<br>2. First dropdown is labeled "Facility".<br>3. Second dropdown is labeled "Payer". |
| 4 | AC4 | Happy Path | High | Resident + Payer Category mode shows Resident and Payer Category dropdowns | Bulk Mode toggle visible, currently on "Facility + Payer" | N/A | 1. Click the "Resident + Payer Category" option on the Bulk Mode toggle.<br>2. Verify the toggle now shows "Resident + Payer Category" as active.<br>3. Verify the label of the first filter dropdown.<br>4. Verify the label of the second filter dropdown. | 1. Click registers on the toggle.<br>2. "Resident + Payer Category" is shown as the active mode.<br>3. First dropdown is labeled "Resident".<br>4. Second dropdown is labeled "Payer Category". |
| 5 | AC5 | Happy Path | Medium | Resident dropdown entries include the facility name | Bulk Mode set to "Resident + Payer Category"; at least one resident exists with an assigned facility | Resident "John Doe" assigned to "Sunrise Facility" | 1. Verify the Resident dropdown is visible.<br>2. Click the Resident dropdown to open its option list.<br>3. Locate the entry for resident "John Doe".<br>4. Verify the facility name is displayed alongside the resident name. | 1. Resident dropdown is visible.<br>2. Dropdown opens showing a list of residents.<br>3. Entry for "John Doe" is present in the list.<br>4. Entry displays both resident name and facility, e.g. "John Doe — Sunrise Facility". |
| 6 | AC6 | Happy Path | High | Switching from Facility + Payer to Resident + Payer Category clears selections | Bulk Mode set to "Facility + Payer" | Facility = "Sunrise Facility"; Payer = "Medicare" | 1. Select "Sunrise Facility" in the Facility dropdown.<br>2. Select "Medicare" in the Payer dropdown.<br>3. Verify both dropdowns show the selected values.<br>4. Click the "Resident + Payer Category" toggle option.<br>5. Verify the Resident dropdown value.<br>6. Verify the Payer Category dropdown value. | 1. Facility dropdown displays "Sunrise Facility".<br>2. Payer dropdown displays "Medicare".<br>3. Both selections are visible.<br>4. Mode switches to "Resident + Payer Category".<br>5. Resident dropdown shows its placeholder/no value selected.<br>6. Payer Category dropdown shows its placeholder/no value selected. |
| 7 | AC6 | Happy Path | Medium | Switching from Resident + Payer Category to Facility + Payer clears selections | Bulk Mode set to "Resident + Payer Category" | Resident = "John Doe"; Payer Category = "Category A" | 1. Select "John Doe" in the Resident dropdown.<br>2. Select "Category A" in the Payer Category dropdown.<br>3. Verify both dropdowns show the selected values.<br>4. Click the "Facility + Payer" toggle option.<br>5. Verify the Facility dropdown value.<br>6. Verify the Payer dropdown value. | 1. Resident dropdown displays "John Doe".<br>2. Payer Category dropdown displays "Category A".<br>3. Both selections are visible.<br>4. Mode switches to "Facility + Payer".<br>5. Facility dropdown shows its placeholder/no value selected.<br>6. Payer dropdown shows its placeholder/no value selected. |
| 8 | AC7 | Happy Path | High | Apply Filters works in Facility + Payer mode | Bulk Mode set to "Facility + Payer"; at least one task exists matching the selected combination | Facility = "Sunrise Facility"; Payer = "Medicare" | 1. Select "Sunrise Facility" in the Facility dropdown.<br>2. Select "Medicare" in the Payer dropdown.<br>3. Verify the Apply Filters button state.<br>4. Click the Apply Filters button. | 1. Facility dropdown shows "Sunrise Facility".<br>2. Payer dropdown shows "Medicare".<br>3. Apply Filters button is enabled.<br>4. Task table refreshes and displays tasks matching the Facility + Payer combination. |
| 9 | AC7 | Happy Path | High | Apply Filters works in Resident + Payer Category mode | Bulk Mode set to "Resident + Payer Category"; at least one task exists matching the selected combination | Resident = "John Doe"; Payer Category = "Category A" | 1. Select "John Doe" in the Resident dropdown.<br>2. Select "Category A" in the Payer Category dropdown.<br>3. Verify the Apply Filters button state.<br>4. Click the Apply Filters button. | 1. Resident dropdown shows "John Doe".<br>2. Payer Category dropdown shows "Category A".<br>3. Apply Filters button is enabled.<br>4. Task table refreshes and displays tasks matching the Resident + Payer Category combination. |
| 10 | AC7 | Happy Path | Medium | Clear button resets filters in Facility + Payer mode | Bulk Mode set to "Facility + Payer"; filters applied and task table populated | Facility = "Sunrise Facility"; Payer = "Medicare" | 1. Select "Sunrise Facility" and "Medicare", then click Apply Filters.<br>2. Verify the task table shows filtered results.<br>3. Click the Clear button.<br>4. Verify the Facility and Payer dropdowns.<br>5. Verify the task table state. | 1. Task table displays filtered results.<br>2. Filtered results are visible.<br>3. Clear button click registers.<br>4. Facility and Payer dropdowns reset to their placeholders.<br>5. Task table returns to its default/unfiltered state. |
| 11 | AC7 | Happy Path | Medium | Clear button resets filters in Resident + Payer Category mode | Bulk Mode set to "Resident + Payer Category"; filters applied and task table populated | Resident = "John Doe"; Payer Category = "Category A" | 1. Select "John Doe" and "Category A", then click Apply Filters.<br>2. Verify the task table shows filtered results.<br>3. Click the Clear button.<br>4. Verify the Resident and Payer Category dropdowns.<br>5. Verify the task table state. | 1. Task table displays filtered results.<br>2. Filtered results are visible.<br>3. Clear button click registers.<br>4. Resident and Payer Category dropdowns reset to their placeholders.<br>5. Task table returns to its default/unfiltered state. |
| 12 | AC8 | Happy Path | High | Task table reflects correct filter logic in Facility + Payer mode | Bulk Mode set to "Facility + Payer"; tasks exist for multiple facility/payer combinations | Facility = "Sunrise Facility"; Payer = "Medicare" | 1. Select "Sunrise Facility" and "Medicare".<br>2. Click Apply Filters.<br>3. Inspect each row of the task table.<br>4. Verify no rows outside the selected Facility/Payer combination are shown. | 1. Filters are selected.<br>2. Task table refreshes.<br>3. Each visible row belongs to "Sunrise Facility" and "Medicare".<br>4. No unrelated rows are present in the table. |
| 13 | AC8 | Happy Path | High | Task table reflects correct filter logic in Resident + Payer Category mode | Bulk Mode set to "Resident + Payer Category"; tasks exist for multiple resident/payer-category combinations | Resident = "John Doe"; Payer Category = "Category A" | 1. Select "John Doe" and "Category A".<br>2. Click Apply Filters.<br>3. Inspect each row of the task table.<br>4. Verify no rows outside the selected Resident/Payer Category combination are shown. | 1. Filters are selected.<br>2. Task table refreshes.<br>3. Each visible row belongs to "John Doe" and "Category A".<br>4. No unrelated rows are present in the table. |
| 14 | AC9 | Negative | High | Apply Filters disabled with only Facility selected | Bulk Mode set to "Facility + Payer"; no prior selections | Facility = "Sunrise Facility"; Payer = none | 1. Select "Sunrise Facility" in the Facility dropdown.<br>2. Leave the Payer dropdown unselected.<br>3. Verify the Apply Filters button state. | 1. Facility dropdown shows "Sunrise Facility".<br>2. Payer dropdown remains at its placeholder.<br>3. Apply Filters button is disabled. |
| 15 | AC9 | Negative | High | Apply Filters disabled with only Payer selected | Bulk Mode set to "Facility + Payer"; no prior selections | Facility = none; Payer = "Medicare" | 1. Leave the Facility dropdown unselected.<br>2. Select "Medicare" in the Payer dropdown.<br>3. Verify the Apply Filters button state. | 1. Facility dropdown remains at its placeholder.<br>2. Payer dropdown shows "Medicare".<br>3. Apply Filters button is disabled. |
| 16 | AC9 | Negative | Medium | Apply Filters disabled with only Resident selected | Bulk Mode set to "Resident + Payer Category"; no prior selections | Resident = "John Doe"; Payer Category = none | 1. Select "John Doe" in the Resident dropdown.<br>2. Leave the Payer Category dropdown unselected.<br>3. Verify the Apply Filters button state. | 1. Resident dropdown shows "John Doe".<br>2. Payer Category dropdown remains at its placeholder.<br>3. Apply Filters button is disabled. |
| 17 | AC9 | Negative | Medium | Apply Filters disabled with only Payer Category selected | Bulk Mode set to "Resident + Payer Category"; no prior selections | Resident = none; Payer Category = "Category A" | 1. Leave the Resident dropdown unselected.<br>2. Select "Category A" in the Payer Category dropdown.<br>3. Verify the Apply Filters button state. | 1. Resident dropdown remains at its placeholder.<br>2. Payer Category dropdown shows "Category A".<br>3. Apply Filters button is disabled. |
| 18 | AC9 | Edge Case | High | Apply Filters becomes enabled only once both dropdowns are populated | Bulk Mode set to "Facility + Payer"; no prior selections | Facility = "Sunrise Facility"; Payer = "Medicare" | 1. Verify the Apply Filters button state before any selection.<br>2. Select "Sunrise Facility" in the Facility dropdown.<br>3. Verify the Apply Filters button state.<br>4. Select "Medicare" in the Payer dropdown.<br>5. Verify the Apply Filters button state. | 1. Apply Filters button is disabled.<br>2. Facility dropdown shows "Sunrise Facility".<br>3. Apply Filters button remains disabled.<br>4. Payer dropdown shows "Medicare".<br>5. Apply Filters button becomes enabled. |
| 19 | AC1-AC9 | RBAC/Permission | High | Viewer role cannot apply bulk filters/actions | User logged in as a basic Viewer role; Navigate to https://revflow-dev.axgsolutions.com/tasks/bulk-edit | N/A | 1. Log in as a Viewer-role user.<br>2. Navigate to the Bulk Edit Tasks screen.<br>3. Verify the Bulk Mode toggle and filter dropdowns are visible.<br>4. Select values in both dropdowns for the active mode.<br>5. Verify the Apply Filters button state. | 1. Login succeeds as Viewer.<br>2. Bulk Edit Tasks screen loads (read-only context).<br>3. Toggle and dropdowns are visible but read-only actions are constrained per role.<br>4. Dropdown selections are accepted for viewing.<br>5. Apply Filters/bulk action button is disabled or hidden for the Viewer role. |
| 20 | AC8 | Edge Case | Medium | Empty state is shown when no tasks match the selected filters | Bulk Mode set to "Facility + Payer"; a Facility/Payer combination exists with zero matching tasks | Facility = "Empty Test Facility"; Payer = "Zero Match Payer" | 1. Select "Empty Test Facility" in the Facility dropdown.<br>2. Select "Zero Match Payer" in the Payer dropdown.<br>3. Click the Apply Filters button.<br>4. Verify the task table content. | 1. Facility dropdown shows "Empty Test Facility".<br>2. Payer dropdown shows "Zero Match Payer".<br>3. Apply Filters button click registers.<br>4. Task table displays a "No tasks found" empty state message and no data rows. |
