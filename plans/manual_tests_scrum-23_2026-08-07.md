# Manual Test Cases — SCRUM-23: FE - Add a Payment Schedule to a Case

> **Duplicate-coverage note:** SCRUM-23's acceptance criteria are identical to ARW-2579
> ("FE - Add a Payment Schedule to a Case"). All scenarios below are already automated in
> `tests/test_arw2579_payment_schedule.py` (13 test functions) using `pages/case_detail_page.py`.
> No new test file was generated for SCRUM-23 — the existing suite was re-run against this
> ticket instead of duplicating coverage. See the "Existing Automation" column for the
> corresponding `test_*` function.

| # | AC | Type | Priority | Scenario | Pre-conditions | Test Data | Steps | Expected Result | Existing Automation |
|---|----|------|----------|----------|----------------|-----------|-------|-----------------|---------------------|
| 1 | AC1 | Happy Path | High | Open modal from empty-state CTA | User logged in; navigate to a case with zero payment schedules | N/A | 1. Navigate to the case detail page for a case with no payment schedules.<br>2. Verify the "Add Payment Schedule" empty-state CTA is visible.<br>3. Click the "Add Payment Schedule" empty-state CTA.<br>4. Verify the modal is visible.<br>5. Verify the modal title element is visible. | 1. Case detail page loads.<br>2. Empty-state CTA is visible.<br>3. CTA is clicked.<br>4. Modal opens.<br>5. Modal title "Add Payment Schedule" and subtitle "Define the expected payment timing and method for a payer. Payments are not initiated from this schedule." are displayed. | `test_pos_open_modal_from_empty_state_cta` |
| 2 | AC1 | Happy Path | High | Open modal from populated-view button | User logged in; navigate to a case with an existing payment schedule row | N/A | 1. Navigate to the case detail page for a case with an existing schedule.<br>2. Verify the "Add Payment Schedule" button is visible in the populated view.<br>3. Click the button.<br>4. Verify the modal is visible. | 1. Case detail page loads with a populated schedule table.<br>2. Button is visible.<br>3. Button is clicked.<br>4. Modal opens with correct title/subtitle. | `test_pos_open_modal_from_populated_view_button` |
| 3 | AC1 | Negative | Medium | Closing modal without saving discards no schedule | Case with an existing schedule row; modal open | N/A | 1. Note the current schedule row count.<br>2. Open the modal via the populated-view button.<br>3. Close the modal without filling/saving.<br>4. Verify modal is no longer visible.<br>5. Verify row count is unchanged. | 1. Row count captured.<br>2. Modal opens.<br>3. Modal closes.<br>4. Modal hidden.<br>5. Row count matches pre-close count. | `test_err_close_modal_discards_no_schedule` |
| 4 | AC2 | Happy Path | High | Payer dropdown shows only eligible payers | Modal open; at least one payer flagged "Allow Payment Schedule" and one not flagged | Eligible payer, ineligible payer | 1. Open the Payer dropdown.<br>2. Verify the eligible payer option is visible.<br>3. Verify the ineligible payer option is NOT present. | 1. Dropdown opens.<br>2. Eligible payer listed.<br>3. Ineligible payer absent from the list. | `test_pos_payer_dropdown_shows_only_eligible_payers` |
| 5 | AC2 | Negative | High | Existing-schedule payer is disabled with tooltip | Case has a payer that already has a schedule | Payer with existing schedule | 1. Open the Payer dropdown.<br>2. Verify the payer-with-existing-schedule option is disabled.<br>3. Hover the disabled option.<br>4. Read the tooltip text. | 1. Dropdown opens.<br>2. Option shown but disabled.<br>3. Tooltip appears on hover.<br>4. Tooltip reads "A payment schedule already exists for this payer." | `test_err_existing_schedule_payer_disabled_with_tooltip` |
| 6 | AC2 | Edge Case | Medium | No eligible payers results in empty dropdown / Save stays disabled | Case with zero payers flagged "Allow Payment Schedule" | N/A | 1. Open modal from empty-state CTA.<br>2. Open the Payer dropdown.<br>3. Verify the Save button remains disabled. | 1. Modal opens.<br>2. Dropdown opens with no eligible options.<br>3. Save button disabled. | `test_err_no_eligible_payers_shows_empty_dropdown` |
| 7 | AC3 | Happy Path | High | "Specific day of the month" reveals day selector | Modal open; eligible payer selected | Schedule Type = "Specific day of the month" | 1. Select an eligible payer.<br>2. Select "Specific day of the month" as Schedule Type.<br>3. Verify the day selector is visible.<br>4. Verify the weekday pattern selector is NOT visible. | 1. Payer selected.<br>2. Schedule Type set.<br>3. Day selector shown.<br>4. Weekday selector hidden. | `test_pos_specific_day_reveals_day_selector` |
| 8 | AC3 | Happy Path | High | "Relative weekday" reveals weekday pattern selector | Modal open; eligible payer selected | Schedule Type = "Relative weekday" | 1. Select an eligible payer.<br>2. Select "Relative weekday" as Schedule Type.<br>3. Verify the weekday pattern selector is visible.<br>4. Verify the day selector is NOT visible. | 1. Payer selected.<br>2. Schedule Type set.<br>3. Weekday selector shown.<br>4. Day selector hidden. | `test_pos_relative_weekday_reveals_weekday_selector` |
| 9 | AC3 | Happy Path | High | All Payment Method options are selectable | Modal open; eligible payer selected | ACH, Credit Card, Direct Deposit, Personal Check, Other | 1. Select an eligible payer.<br>2. Open the Payment Method dropdown.<br>3. Verify each of the 5 expected options is present. | 1. Payer selected.<br>2. Dropdown opens.<br>3. ACH, Credit Card, Direct Deposit, Personal Check, and Other are all listed. | `test_pos_all_payment_methods_selectable` |
| 10 | AC3 | Happy Path | Medium | Auto-Pay checkbox toggles and shows helper text | Modal open; eligible payer selected | N/A | 1. Verify the Auto-Pay helper text "Indicates whether this payer is set up for auto-pay." is visible.<br>2. Click the Auto-Pay checkbox to check it.<br>3. Verify checkbox is checked.<br>4. Click again to uncheck.<br>5. Verify checkbox is unchecked. | 1. Helper text visible.<br>2. Checkbox clicked.<br>3. Checkbox checked.<br>4. Checkbox clicked again.<br>5. Checkbox unchecked. | `test_pos_autopay_checkbox_toggle_and_helper_text` |
| 11 | AC3/AC6 | Negative | High | Save button disabled until all required fields complete | Modal open | Payer, Schedule Type "Specific day of the month" + day 15, Payment Method "ACH", Auto-Pay | 1. Verify Save disabled with no fields filled.<br>2. Select payer; verify Save still disabled.<br>3. Select schedule type + day; verify Save still disabled.<br>4. Select payment method; verify Save still disabled.<br>5. Set Auto-Pay status; verify Save becomes enabled. | 1. Save disabled.<br>2. Still disabled.<br>3. Still disabled.<br>4. Still disabled.<br>5. Save becomes enabled only once every required field is set. | `test_err_save_disabled_until_required_fields_complete` |
| 12 | AC4/AC8 | Happy Path | High | Save shows success toast and updates table | Modal open; all required fields fillable | Payer, "Specific day of the month" + day 15, "ACH", Auto-Pay on | 1. Note current schedule row count.<br>2. Fill all required fields.<br>3. Click Save.<br>4. Verify modal closes.<br>5. Verify row count increased by 1.<br>6. Verify success toast is visible. | 1. Row count captured.<br>2. Fields filled.<br>3. Save clicked.<br>4. Modal closes.<br>5. New row appears.<br>6. Toast reads "Payment schedule added successfully." | `test_pos_save_schedule_success_toast_and_table_update` |
| 13 | AC4/AC7 | Negative | High | Duplicate schedule prevented for same payer | Case has a payer with an existing schedule | Payer with existing schedule | 1. Open the Payer dropdown.<br>2. Verify the payer with an existing schedule cannot be selected (disabled). | 1. Dropdown opens.<br>2. Option is disabled, preventing a second schedule for that payer/case pair. | `test_err_duplicate_schedule_prevented_for_same_payer` |
| 14 | AC1 (RBAC) | RBAC/Permission | High | Viewer role cannot access Add Payment Schedule | Logged in as a Viewer-role user; case with existing schedule | N/A | 1. Navigate to the case detail page as a Viewer.<br>2. Verify the "Add Payment Schedule" entry point is hidden or disabled. | 1. Page loads.<br>2. Entry point is either not rendered or rendered but disabled — Viewer cannot create a schedule. | `test_perm_viewer_cannot_access_add_payment_schedule` |
