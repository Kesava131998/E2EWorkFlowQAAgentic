# Manual Test Cases — ARW-2579: FE - Add a Payment Schedule to a Case

Jira: https://axg.atlassian.net/browse/ARW-2579
Figma: https://www.figma.com/design/hTnMqvwgU7TqM9RHlM63MW/UI-Edit--Case-view---Payment-schedule

| # | AC | Type | Priority | Scenario | Pre-conditions | Test Data | Steps | Expected Result |
|---|----|------|----------|----------|----------------|-----------|-------|-----------------|
| 1 | AC1 | Happy Path | High | Open "Add Payment Schedule" modal from empty-state CTA | User logged in as Admin/Billing user; navigate to a Case with no existing payment schedules | None | 1. Navigate to case detail view for a case with zero payment schedules.<br>2. Verify the empty-state "Add Payment Schedule" CTA is visible.<br>3. Click the "Add Payment Schedule" CTA.<br>4. Verify the modal is visible. | 1. Case detail view for the target case is loaded.<br>2. Empty-state CTA is visible and accessible.<br>3. CTA is clicked.<br>4. Modal opens with title "Add Payment Schedule" and subtitle "Define the expected payment timing and method for a payer. Payments are not initiated from this schedule." |
| 2 | AC1 | Happy Path | High | Open "Add Payment Schedule" modal from populated view button | User logged in as Admin/Billing user; navigate to a Case with at least one existing payment schedule | None | 1. Navigate to case detail view for a case with an existing payment schedule.<br>2. Verify the "Add Payment Schedule" button is visible in the populated table view.<br>3. Click the "Add Payment Schedule" button.<br>4. Verify the modal is visible. | 1. Case detail view is loaded showing existing schedule(s) in a table.<br>2. Button is visible and accessible.<br>3. Button is clicked.<br>4. Modal opens with title "Add Payment Schedule" and subtitle text. |
| 3 | AC2 | Happy Path | High | Payer dropdown only lists payers flagged "Allow Payment Schedule" | Case has a mix of payers with/without the "Allow Payment Schedule" category flag | Case with 2 eligible + 1 ineligible payer | 1. Open "Add Payment Schedule" modal.<br>2. Verify the Payer dropdown is visible.<br>3. Click the Payer dropdown.<br>4. Verify the list of options shown. | 1. Modal is open.<br>2. Payer dropdown is visible and accessible.<br>3. Dropdown opens showing payer options.<br>4. Only payers flagged "Allow Payment Schedule" appear in the list; ineligible payer is absent. |
| 4 | AC2 | Negative | Medium | Payer with an existing schedule for this case is disabled with tooltip | Case has a payer that already has a payment schedule | Payer "Payer A" (existing schedule) | 1. Open "Add Payment Schedule" modal.<br>2. Click the Payer dropdown.<br>3. Verify "Payer A" option is present but disabled.<br>4. Hover over "Payer A" option. | 1. Modal is open.<br>2. Dropdown options are visible.<br>3. "Payer A" is rendered in a disabled state and cannot be selected.<br>4. Tooltip "A payment schedule already exists for this payer." is displayed. |
| 5 | AC2 | Happy Path | High | Selecting Schedule Type "Specific day of the month" reveals day selector | Modal open, eligible payer available | Schedule Type = "Specific day of the month" | 1. Open "Add Payment Schedule" modal.<br>2. Select an eligible payer.<br>3. Select Schedule Type "Specific day of the month".<br>4. Verify the day selector field is visible. | 1. Modal is open.<br>2. Payer is selected.<br>3. Schedule Type is set to "Specific day of the month".<br>4. Day selector (1–31) is visible and accessible. |
| 6 | AC2 | Happy Path | High | Selecting Schedule Type "Relative weekday" reveals weekday pattern selector | Modal open, eligible payer available | Schedule Type = "Relative weekday" | 1. Open "Add Payment Schedule" modal.<br>2. Select an eligible payer.<br>3. Select Schedule Type "Relative weekday".<br>4. Verify the weekday pattern selector (e.g., ordinal + day-of-week) is visible. | 1. Modal is open.<br>2. Payer is selected.<br>3. Schedule Type is set to "Relative weekday".<br>4. Weekday pattern selector (e.g., "3rd Thursday", "last Wednesday") is visible and accessible. |
| 7 | AC2 | Happy Path | High | Payment Method dropdown lists all five defined methods | Modal open | None | 1. Open "Add Payment Schedule" modal.<br>2. Click the Payment Method dropdown.<br>3. Verify each option is present: ACH, Credit Card, Direct Deposit, Personal Check, Other. | 1. Modal is open.<br>2. Payment Method dropdown opens.<br>3. All five options (ACH, Credit Card, Direct Deposit, Personal Check, Other) are visible and selectable. |
| 8 | AC2 | Edge Case | Medium | Auto-Pay Status checkbox toggles and shows helper text | Modal open | None | 1. Open "Add Payment Schedule" modal.<br>2. Verify Auto-Pay Status checkbox is visible, unchecked by default.<br>3. Click the Auto-Pay Status checkbox.<br>4. Verify helper text is displayed. | 1. Modal is open.<br>2. Checkbox is visible and unchecked.<br>3. Checkbox becomes checked.<br>4. Helper text "Indicates whether this payer is set up for auto-pay." is visible. |
| 9 | AC3 | Negative | High | Save button remains disabled until all required fields are completed | Modal open, no fields filled | Partial data (Payer only) | 1. Open "Add Payment Schedule" modal.<br>2. Verify Save button is disabled.<br>3. Select a Payer only.<br>4. Verify Save button is still disabled. | 1. Modal is open.<br>2. Save button is visible and disabled.<br>3. Payer field is populated.<br>4. Save button remains disabled since Schedule Type, Schedule Details, and Payment Method are incomplete. |
| 10 | AC3 | Happy Path | High | Save button becomes enabled once all required fields are completed | Modal open, eligible payer available | Payer=Payer B, Schedule Type=Specific day, Day=15, Payment Method=ACH, Auto-Pay=unchecked | 1. Open "Add Payment Schedule" modal.<br>2. Fill Payer, Schedule Type, Schedule Details, and Payment Method fields.<br>3. Verify Save button state. | 1. Modal is open.<br>2. All required fields show the entered/selected values.<br>3. Save button becomes enabled. |
| 11 | AC3 | Negative | High | System prevents saving more than one payment schedule for the same payer on a case | Case already has a saved schedule for "Payer A" | Attempt second schedule for "Payer A" | 1. Open "Add Payment Schedule" modal.<br>2. Attempt to select "Payer A" in the Payer dropdown.<br>3. Verify selection is blocked. | 1. Modal is open.<br>2. "Payer A" appears disabled in the dropdown (per test case 4) and cannot be chosen.<br>3. No duplicate schedule can be created for "Payer A" on this case. |
| 12 | AC4 | Happy Path | High | Successful save closes modal, adds row to table, and shows success toast | Modal open, all required fields valid | Payer=Payer C, Schedule Type=Relative weekday, Pattern="3rd Thursday", Payment Method=Direct Deposit, Auto-Pay=checked | 1. Open "Add Payment Schedule" modal.<br>2. Fill all required fields with valid data.<br>3. Click Save.<br>4. Verify modal closes.<br>5. Verify new row appears in the payment schedule table.<br>6. Verify success toast is shown. | 1. Modal is open.<br>2. All fields reflect entered values.<br>3. Save is clicked.<br>4. Modal is no longer visible.<br>5. New schedule row for Payer C is visible in the table with the entered details.<br>6. Toast message "Payment schedule added successfully." is displayed. |
| 13 | AC2 | Permission | Medium | Viewer-role user cannot access "Add Payment Schedule" action | User logged in as a read-only Viewer role | None | 1. Log in as a Viewer-role user.<br>2. Navigate to the case detail view.<br>3. Verify the "Add Payment Schedule" CTA/button state. | 1. Viewer user is logged in.<br>2. Case detail view loads.<br>3. "Add Payment Schedule" CTA/button is disabled or hidden for the Viewer role. |

**Note on Test 13:** the ticket does not explicitly define RBAC rules for this feature — this case is included per project convention to cover access-control regression and should be validated/adjusted against actual role definitions before automation.
